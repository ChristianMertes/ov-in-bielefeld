services:
  web:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - db-data:/data
      - log-data:/app/logs
    env_file: .env
    restart: unless-stopped

  scheduler:
    build: .
    volumes:
      - db-data:/data
      - log-data:/app/logs
    env_file: .env
    restart: unless-stopped
    command: uv run python scheduler.py
    depends_on:
      web:
        condition: service_healthy

volumes:
  db-data:
  log-data:
